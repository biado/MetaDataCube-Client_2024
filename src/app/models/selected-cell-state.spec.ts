import { SelectedCellState } from './selected-cell-state';

describe('SelectedCellState', () => {
  it('should create an instance', () => {
    expect(new SelectedCellState()).toBeTruthy();
  });
});
